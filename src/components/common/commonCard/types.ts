export type cardType = {
  id: number;
  title: string;
  body: string;
};

export type classesType = {
  headingClasses: string;
  cardNumberClasses: string;
  titleClasses: string;
  bodyClasses: string;
};
